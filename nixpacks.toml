[phases.setup]
nixPkgs = ['php82', 'php82Extensions.gd', 'php82Extensions.pdo', 'php82Extensions.pdo_mysql']

[phases.build]
cmds = ['composer install --no-dev --optimize-autoloader']

[start]
cmd = 'php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=8000'